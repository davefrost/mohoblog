# Test Environment Variables
NODE_ENV=test
DATABASE_URL=postgresql://test:test@localhost:5432/test_motorhome_blog
TEST_DATABASE_URL=postgresql://test:test@localhost:5432/test_motorhome_blog

# Auth (can be dummy for tests)
REPLIT_DB_URL=memory://
SESSION_SECRET=test-session-secret-for-testing-only

# Email (dummy for tests)  
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASS=testpassword
FROM_EMAIL=test@example.com